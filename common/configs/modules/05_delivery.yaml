# 05_delivery 模块配置
oss:
  endpoint: ""
  bucket: ""
  access_key_id: ""
  access_key_secret: ""
  region: "cn-hangzhou"

delivery:
  enable_notification: true
  notification_channels: ["email", "webhook"]
  retry_attempts: 3
  timeout_seconds: 300

security:
  encrypt_data: false
  verify_checksum: true